// u may know it as a object, may be a map,dictionary or
//  sometimes a HashTable.They all are basically the same thing

// const person = {}
// person['firstName'] = 'bob';
// person['lastName'] = 'bobby';
// console.log(person['firstName']);

class HashTable {

    table = new array(100)

    setItem = (key, value) => {

    }

    getItem = (key) => {
        return '';
    }
}
const myTable = new HashTable();

myTable.setItem('firstName', 'bob');
myTable.getItem('firstName');

console.log(myTable.getItem("firstName"));


